## What this changes

<!-- One or two sentences. Link the issue if one exists. -->

## Checklist

- [ ] `npm test`, `npm run lint`, and `npm run typecheck` pass
- [ ] `npx drafteine check --all` passes (new files are declared in structure.dft)
- [ ] `npx drafteine fmt structure.dft --check` passes if the contract changed
- [ ] Code prose follows the style rules in CONTRIBUTING.md
- [ ] Language or syntax changes were discussed in an issue first
